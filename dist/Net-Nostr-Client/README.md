# Net::Nostr::Client

WebSocket client implementation for Nostr relays.

This distribution depends on `Net::Nostr::Core` for protocol objects and message
handling. Install `Net::Nostr` instead if you want both the client and relay
implementations.
