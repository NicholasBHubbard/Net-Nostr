# Net::Nostr::Relay

WebSocket relay server implementation for the Nostr protocol.

This distribution depends on `Net::Nostr::Core` for protocol objects, message
parsing, relay information documents, and validation helpers. Install
`Net::Nostr` instead if you want both the relay and client implementations.
