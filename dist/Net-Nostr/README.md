# Net::Nostr

Compatibility shim for the Nostr Perl distributions.

Installing this distribution installs `Net::Nostr::Core`,
`Net::Nostr::Client`, and `Net::Nostr::Relay`, plus `AnyEvent::HTTP` for
full NIP-05 HTTP lookup and verification support.
